# XML 导入编译基线

测试方式：每个样例均导入独立临时 S7-1200 工程并调用真实 TIA V17 编译器。

| 样例 | 来源 | XML解析 | TIA导入 | TIA编译 | 警告数 | 错误数 | 是否经过修复 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Main.xml` | `application` | 通过 | 通过 | 失败 | 0 | 6 | 否 | 缺少风机燃气、烧嘴控制块等 6 个被调用块；该结果为真实基线。 |
| `01_串联_触点线圈.xml` | `basic` | 通过 | 通过 | 失败（预期） | 0 | ≥1 | 否 | 引用不存在的 `DB1` 符号，测试确认不会误判成功。 |
| `SCDW_TEST_FC.scl` | 测试 fixture | 不适用 | 通过 | 通过 | 0 | 0 | 不适用 | 最小 FC 用于程序块导入和成功编译路径。 |

现阶段不修改模板或 XML 生成逻辑以改变这些结果。
