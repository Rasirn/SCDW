# 定时器与计数器实例规则

- `verified_v17_sample`：TON 样本使用 `Version="1.0"`、IEC_TIMER 实例和 `time_type` TemplateValue；全局实例与 FB Static 本地实例分别有独立知识项。
- `verified_v17_sample`：本地定时器 Instance 的名称必须在调用方 Static Interface 中存在。
- `verified_v17_sample`：当前 application 未发现 TOF、TP 或 IEC 计数器样本，因此知识库不臆造其端口、实例或 Version。
